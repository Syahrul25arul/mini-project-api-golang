# links [api documentation](https://syahrul25arul.github.io/mini-project-golang-documentation/)

##### for admin account, use `username = admin` and `password = admin` for login routes

> sql database file in folder database
